INI TUTOR UNTUK VPS

1️⃣ Get Proxy :

- buka vps kalian
- Install python   : apt install python3
- Download this program   : https://github.com/monosans/proxy-scraper-checker/archive/refs/heads/main.zip and unzip to Downloads
- setelah di unzip, pindahkan file yg ada di lokal penyimpanan mu ke vps kalian
- buat screen dengan cara screen -R grass
- masuk ke folder cd proxy-scraper-checker-main
- ketik chmod +x ./start.sh
- lalu ketik ./start.sh
- lalu tunggu hingga proses selesai
- jika sudah masuk ke direktori dengan cd proxy-scraper-checker-main/out/proxies
- lalu copy file all.txt dengan cara berikut 
- cp all.txt /root/getgrass-main/
- lalu masuk ke /getgrass-main lalu hapus file proxy list.txt 
- ganti nama file all.txt yg tadi sudah di copy menjadi proxy list.txt (ini bertujuan untuk meng update proxy lama menjadi baru)

➡️ 2️⃣ Running Bot :

- Download or git clone this repo https://github.com/im-hanzou/getgrass/archive/refs/heads/main.zip, if download unzip
- setelah di unzip, pindahkan file yg ada di lokal penyimpanan mu ke vps kalian
- ketik cd $home di command
- lalu masuk ke folder grass dengan cara cd getgrass-main
- lalu jalan kan command berikut pip install fake_useragent && pip install loguru && pip install websockets_proxy && pip install aiohttp && pip install aiodns and enter
- setelah ini, kalian bikin akun grass dulu di chrome 

➡️ 3️⃣Create account :

- Create new account for Safety : https://app.getgrass.io/register/?referralCode=MRjJGN6AZJGgJwQ
- Go to https://app.getgrass.io/dashboard and press F12 atau klik kanan lalu cari inspect element
- Go to console, type allow pasting and enter, type localStorage.getItem('userId') and enter
- Copy your userId without " to " for example '"kdjnf2138-214sa-489n-jfi213-akslnmdfi314"' just copy kdjnf2138-214sa-489n-jfi213-akslnmdfi314 to last step running bot
- lalu masuk lagi ke vps kalian yg sudah d install tadi (untuk lebih jelasnya bisa lihat di video)
- lalu ketik python3 run.py and paste your userId from last step Create account (if error python3 1user_id.py)
- waiting until process done, if showing "could not connect to proxy" just wait until 1 - 3 hours, it's normal 😎

Source :
https://github.com/im-hanzou/getgrass
